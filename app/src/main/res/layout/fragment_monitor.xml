<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_app_gray"
    tools:context=".presentation.monitor.MonitorFragment">

                <!-- Header biru dengan lengkungan di bawah -->
                <View
                    android:id="@+id/blueHeaderBackground"
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:background="@drawable/bg_monitor_header"
                    app:layout_constraintBottom_toBottomOf="@id/headerBottomGuideline"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />

                <!-- Guideline untuk menentukan batas bawah header (TabLayout + 8dp) -->
                <androidx.constraintlayout.widget.Guideline
                    android:id="@+id/headerBottomGuideline"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    app:layout_constraintGuide_begin="140dp" />

                <!-- Toolbar -->
                <com.google.android.material.appbar.MaterialToolbar
                    android:id="@+id/toolbar"
                    android:layout_width="0dp"
                    android:layout_height="?attr/actionBarSize"
                    android:background="@android:color/transparent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:title="Monitor Loket"
                    app:titleTextColor="@color/yellow_secondary_accent" />

                <!-- TabLayout dengan efek mengambang -->
                <com.google.android.material.tabs.TabLayout
                    android:id="@+id/tabLayout"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginHorizontal="16dp"
                    android:layout_marginTop="8dp"
                    android:layout_marginBottom="8dp"
                    android:background="@drawable/tab_background"
                    android:elevation="4dp"
                    android:paddingVertical="4dp"
                    app:layout_constraintBottom_toBottomOf="@id/headerBottomGuideline"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/toolbar"
                    app:tabBackground="@drawable/tab_item_background"
                    app:tabIndicatorHeight="0dp"
                    app:tabMode="fixed"
                    app:tabSelectedTextColor="@color/text_primary_black"
                    app:tabTextColor="@color/text_primary_black" />

                <!-- ViewPager untuk konten tab -->
                <androidx.viewpager2.widget.ViewPager2
                    android:id="@+id/viewPager"
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:layout_marginTop="16dp"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toBottomOf="@id/blueHeaderBackground" />

        </androidx.constraintlayout.widget.ConstraintLayout>