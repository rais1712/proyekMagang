<?xml version="1.0" encoding="utf-8"?>
<!-- File: app/src/main/res/layout/item_loket_placeholder.xml -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="24dp"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Placeholder for loket name -->
        <View
            android:id="@+id/placeholderLoketName"
            android:layout_width="0dp"
            android:layout_height="20dp"
            android:background="@color/stroke_thin_gray"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Placeholder for phone number -->
        <View
            android:id="@+id/placeholderPhone"
            android:layout_width="0dp"
            android:layout_height="16dp"
            android:layout_marginTop="8dp"
            android:background="@color/stroke_thin_gray"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/placeholderLoketName"
            app:layout_constraintWidth_percent="0.7" />

        <!-- Placeholder for loket number -->
        <View
            android:id="@+id/placeholderLoketNumber"
            android:layout_width="0dp"
            android:layout_height="14dp"
            android:layout_marginTop="6dp"
            android:background="@color/stroke_thin_gray"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/placeholderPhone"
            app:layout_constraintWidth_percent="0.5" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>